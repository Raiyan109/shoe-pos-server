import express, { NextFunction, Request, Response } from 'express';


const router = express.Router();

// router.get('/get-all-conversation', ClientController.getAllUsers);

export const ConversationRoutes = router;